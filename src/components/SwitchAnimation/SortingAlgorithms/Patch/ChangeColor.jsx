export default function changeColor(arr, i, color) {
    arr[i].backgroundColor = color;
  
    /*
    if (arr[i].backgroundColor == color) {
      arr[i].backgroundColor = COLORS.original;
    } else {
      arr[i].backgroundColor = color;
    }
    */ 
  }

  